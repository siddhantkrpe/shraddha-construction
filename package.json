{
  "name": "shraddha-construction",
  "version": "1.0.0",
  "private": true,
  "description": "Shraddha Construction finance tracker",
  "packageManager": "npm@11",
  "scripts": {
    "start": "npx serve .",
    "dev": "npx live-server --port=5173",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "dependencies": {
    "@supabase/ssr": "^0.12.0",
    "@supabase/supabase-js": "^2.108.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
